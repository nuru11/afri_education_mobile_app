const authTokenStorage = 'authTokenStorage';
const defaultApiURL = 'https://api.entrancetricks.com';

/// Must match backend `app_package` (e.g. grades, branding). May differ from
/// [PackageInfo.packageName] on Android when the store applicationId is
/// `com.remedialtricks.app`.
const String backendAppPackage = 'com.vector_academy.app';
// const defaultApiURL = 'http://192.168.1.4:8000';
